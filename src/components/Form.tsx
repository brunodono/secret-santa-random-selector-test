const Form = () => {
    return (
        <form>
            <input type="text" placeholder="Enter the participants's names"></input>
            <button disabled={true}>Add</button>
        </form>

    )


}

export default Form;